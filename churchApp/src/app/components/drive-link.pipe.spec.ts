import { DriveLinkPipe } from './drive-link.pipe';

describe('DriveLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new DriveLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
